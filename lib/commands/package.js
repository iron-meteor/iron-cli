/*
PackageCommand = EM.Command.create({
  name: 'package',

  description: 'Useful utilites for working with packages.',

  usage: 'em package <command> [opts]',

  handler: function (args, opts) {
    var name = args[0];

    if (EM.hasPackage(name))
      EM.success("Yep, you've got that package");
    else
      EM.warn("Nope, don't have that package");
  }
});

module.exports = PackageCommand;
*/
