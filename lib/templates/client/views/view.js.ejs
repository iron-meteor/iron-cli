<% if(realExtension === '.js' || realExtension === '.ts') { %>
/*****************************************************************************/
/* <%= name %>: Event Handlers and Helpersss <%= realExtension %>*/
/*****************************************************************************/
Template.<%= name %>.events({
  /*
   * Example:
   *  'click .selector': function (e, tmpl) {
   *
   *  }
   */
});

Template.<%= name %>.helpers({
  /*
   * Example:
   *  items: function () {
   *    return Items.find();
   *  }
   */
});

/*****************************************************************************/
/* <%= name %>: Lifecycle Hooks */
/*****************************************************************************/
Template.<%= name %>.created = function () {
};

Template.<%= name %>.rendered = function () {
};

Template.<%= name %>.destroyed = function () {
};

<% } else { %>
# Test: Event Handlers and Helpers
Template.Test.events
# Example:
#  "click .selector": (e, tmpl) ->
#

Template.Test.helpers items: ->
# Example:
#  Items.find()
#

# Test: Lifecycle Hooks
Template.Test.created = ->

Template.Test.rendered = ->

Template.Test.destroyed = ->
<% } %>