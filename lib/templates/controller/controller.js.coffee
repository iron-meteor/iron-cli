@<%= name %>Controller = RouteController.extend(
  subscriptions: ->
  waitOn: ->
  data: ->
  onRun: ->
  onRerun: ->
  onBeforeAction: ->
  onAfterAction: ->
  onStop: ->
  action: ->
    @render()
    return
)
