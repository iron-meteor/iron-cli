<% if(realExtension === '.coffee') { %>
@<%= name %>Controller = RouteController.extend(
  waitOn: ->

  data: ->

  action: ->
    @render()
    return
)
<% } else { %>
<%= name %>Controller = RouteController.extend({
  waitOn: function () {
  },

  data: function () {
  },

  action: function () {
    this.render();
  }
});
<% } %>
