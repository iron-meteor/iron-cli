<% if(realExtension === '.coffee') { %>
#
# Add query methods like this:
#  <%= name %>.findPublic = ->
#    <%= name %>.find is_public: true
#
<% } else { %>
/*
 * Add query methods like this:
 *  <%= name %>.findPublic = function () {
 *    return <%= name %>.find({is_public: true});
 *  }
 */
<% } %>